import { Panel } from '../components'

/** Placeholder — real content ships in STORY-015e. */
export function CheckHistoryPage() {
  return (
    <Panel title="Check History" headingLevel="h1">
      <p className="text-body">Check history lands in STORY-015e.</p>
    </Panel>
  )
}
