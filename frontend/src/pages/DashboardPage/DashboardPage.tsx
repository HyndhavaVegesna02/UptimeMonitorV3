import { PlaceholderPage } from '../../components/PlaceholderPage/PlaceholderPage'

/** STORY-121: placeholder. STORY-122 fills this in with the real KPI row,
 * response-time chart, probe-locations panel, and recent-checks feed. */
export function DashboardPage() {
  return (
    <PlaceholderPage description="The system overview — KPIs, response-time chart, probe locations, and recent checks." />
  )
}
