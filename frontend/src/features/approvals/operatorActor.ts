/**
 * The single fixed operator identity every decision on this page is
 * submitted as (STORY-131 AC2 — "actor is a single fixed operator
 * constant, one swap-point for future auth"). Real auth is out of scope
 * for this sprint; when it lands, this is the one place that changes.
 */
export const OPERATOR_ACTOR = 'dashboard-operator'
