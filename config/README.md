# config/

Reserved for system topology and seed configuration, kept **outside** `backend/`
on purpose (dossier §4/§7): editing config is a topology change, not a code
change, and that separation should be visible at the directory level.

Empty placeholder for now — topology/seed files arrive in a later story.
