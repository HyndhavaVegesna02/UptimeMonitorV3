"""STORY-182 — the demo-loop reality-gate harness (part 2b of the Grail demo engine).

Dev-only, under `tools/` (never `backend/src/`): builds the fleet-wide
coverage artifact (B1), the publisher-chain discrimination check (reality
gate side 2), the backfill discrimination check (side 3), and the
multi-process run-and-terminate harness (side 1) that drives the unmodified
`python -m src.composition.run` against the demo engine.

See `docs/scrum/stories/STORY-182-demo-loop-run-and-gate.md` and
`docs/scrum/wiki/demo-engine.md`.
"""
