# YourTeam active sprint board.
sprint: 6
status: active           # none | active | review | closed | aborted
started: 2026-06-25
goal: "Zone 4 opens — the first two pure pipeline stages (collapse: per-location observations
  to one verdict, maintenance excluded; streak: consecutive same-health verdicts) land as
  provider-blind core logic, and the three carried 1-pt chores clear the small debt."
branch: sprint-6
start_tag: sprint-6-start
# Capacity: velocity history (8, 6, 6, 6, 5, 6), last-3 mean (6, 5, 6) = ~6. Committed 6.
# STORY-010 was SPLIT at refinement (the original 4-stage pipeline read as an 8): this sprint
# takes stages 1-2 (collapse + streak, 3 pts, pure, no config/proposals); stages 3-4 (anti-flap
# + decide) are STORY-024 (draft, for later). Plus the three carried chores (021/022/023).
# Order: STORY-010 FIRST (the high-risk new Zone 4 work — a blocker still leaves time for the
# 1-pt chores), then 021, 022, 023.
# Green DoD baseline re-verified on main @ f543b5a before lock: pytest 133, lint-imports
# 3 kept/0 broken, alembic upgrade head exit 0 (Dockerized postgres:16), check_fk_direction
# 10 checked/0 violations.
# Model assignment (PO rule, mandatory): implementers -> Sonnet, reviewers -> Opus.
stories:
  - id: STORY-010
    points: 3
    board: in-progress     # todo | in-progress | blocked | review | done
    pipeline: full         # 3 pts -> implementer + spec reviewer + quality reviewer + DoD gate
    spec_review: PASS       # Opus: all 4 AC MET — collapse health rule, maintenance exclusion/
                            # short-circuit, streak backward + maintenance-gap skip, purity. Tests
                            # assert expected values incl. the tricky maintenance-between-same-health case.
    quality_review: FIX_REQUIRED  # Opus, fix loop 1: 1 MAJOR — collapse([]) on an empty observation
                            # list raises a leaky `max() iterable argument is empty` (and would
                            # IndexError on signal_key) instead of a clear domain error; the brief
                            # flagged this case and streak([]) IS handled, so collapse's gap is
                            # conspicuous. 2 minors recorded as story notes (non-blocking): (1) the
                            # Verdict maintenance<=>health-is-None invariant is documented but not
                            # enforced (a model_validator would close it, consistent with signal.py);
                            # (2) Streak.health non-optional while streak()->Streak|None — judged fine.
    blocked_question: null
    attempts: 1             # fix loop 1 (fresh agent per working agreement), not a fresh attempt
    paused_at_commit: 87f20d9
    dod_evidence: []        # cleared — re-run the full gate after the fix-loop commit
    blast_radius_resolved: false   # re-resolve at the post-fix DoD gate. canonical-types-and-ports.md
                                   # was updated by the implementer (Verdict + pipeline); re-verify +
                                   # re-stamp to the final fix commit.
  - id: STORY-021
    points: 1
    board: todo
    pipeline: light        # 1-2 pts -> implementer + DoD gate (no reviewers)
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false   # diff touches dynatrace query.py -> re-verify dynatrace-adapter.md
  - id: STORY-022
    points: 1
    board: todo
    pipeline: light
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false   # diff touches core ingest_service.py -> re-verify
                                   # ingest-service-and-pull-loop.md (the single-signal-batch Fact).
  - id: STORY-023
    points: 1
    board: todo
    pipeline: light
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false   # comment-only change in pull_loop.py -> re-verify
                                   # ingest-service-and-pull-loop.md (no Fact change expected).
hotfixes: []
interrupts: []
