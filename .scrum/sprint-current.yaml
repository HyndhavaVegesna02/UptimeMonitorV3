# YourTeam active sprint board.
sprint: 25
status: active        # none | active | review | closed | aborted
started: 2026-07-02
closed: null
goal: "Stand the frontend zone back up — second attempt, new design direction. Sprint 23-24's UI
  (DESIGN-airtable.md era) was fully reverted by the PO in 521764c; this sprint rebuilds the
  frontend shell (STORY-015a) guided by DESIGN-linear.app.md as a reference to think with, not
  copy: Vite+React+TS strict SPA, six-tab nav + routing, typed API client + MSW/Vitest harness,
  a theme-scoped token layer (dark = reference-native, light = derived; PO chose BOTH with a
  toggle), shell primitives (Button/StatusBadge/Panel/states), health tokens for both themes,
  and the three frontend DoD gates activated. Every later per-tab story (015b-015g) then just
  fills in one tab."
branch: sprint-25
start_tag: sprint-25-start
# NUMBERING: sprints 23/24 happened and were reverted (521764c); their branches preserve that
# history, so this sprint is 25 — the numbers 23/24 are burned, velocity.json simply has no
# entries for them.
#
# Capacity: velocity last-3 (sprints 20,21,22) = 5,5,3 -> mean 4.33. Committed 5 (STORY-015a) —
# a deliberate single-story focused sprint (same shape as sprint 22); the shell is the
# foundation everything else stacks on, so it gets the whole sprint.
#
# PO DECISIONS AT REFINEMENT (2026-07-02): (1) theme = dark + light with toggle; (2) working
# agreements start clean — the two reverted sprint-23/24 amendments were NOT re-adopted (their
# lessons are baked into story AC instead: AC1 scaffold pruning, AC6 non-text-only health
# color); (3) external implementation per the standing 2026-06-26 agreement; (4) split + shell-
# first approved. DESIGN-linear.app.md is a GUIDE (creative freedom), not a verbatim mandate —
# unlike the Airtable era.
#
# WORKFLOW (amended at lock, PO directive 2026-07-02): implementation is IN-PROCESS — a
# Sonnet 5 implementer subagent (model "sonnet", briefed for high effort) builds to plan.md
# as the self-contained contract, committing onto sprint-25. This supersedes the 2026-06-26
# external-handoff mode. The orchestrator then runs the FULL gate (six backend commands +
# the three new frontend commands), the Opus spec + quality reviewers (5 pts -> full
# pipeline), the mechanical wiki blast-radius sweep, then review/verdict/merge/retro.
#
# DoD: backend six commands UNCHANGED and must stay green (no backend source change in scope).
# Frontend gates (npm test / npm run build / npm run lint) become REAL in this story (AC7) —
# activated in .scrum/definition-of-done.md + CLAUDE.md in the same commit (command-sync).
#
# BASELINE at lock: branch sprint-25 cut from main @ 521764c. All six backend gates verified
# green there this session (2026-07-02): pytest 426 passed (throwaway postgres:16 via
# dev_db.py), lint-imports 5 kept / 0 broken, check_fk_direction 11 FKs / 0 violations,
# python -m alembic upgrade head exit 0, ruff check clean, ruff format --check 153 files clean.
# (Note: alembic runs via `python -m alembic` on this box — the alembic.exe shim is blocked by
# an Application Control policy.)
#
# Agreements in force: all of working-agreements.md as of 521764c (the sprint-23/24 frontend
# amendments were reverted and NOT re-adopted — PO chose start-clean at 2026-07-02 refinement).
stories:
  - id: STORY-015a
    points: 5
    board: in-progress     # todo | in-progress | blocked | review | done
    pipeline: gate+reviewers   # 5 pts -> Sonnet 5 impl + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 1
    paused_at_commit: null
    dod_evidence: []
    review: null
    blast_radius_resolved: false
hotfixes: []
interrupts: []
