# YourTeam active sprint board.
sprint: 21
status: closed        # none | active | review | closed | aborted
started: 2026-06-29
closed: 2026-06-29
goal: "Reconcile the Dynatrace ingest path to the PO's REAL Grail schema (proven by a live probe) and
  verify the pipeline end to end INTERNALLY (real monitor -> observations -> pipeline -> proposal via
  the API). Statuspage is OUT of scope (PO decision: the publish chain is coded + fixture-tested, so it
  follows once a valid key exists). Real fixes: data object dt.synthetic.events + filter
  dt.synthetic.monitor.id; async query (202 -> poll query:poll until SUCCEEDED); real field remap incl.
  9-digit nanosecond timestamps + ns->ms latency; dispatch on event.type; Dynatrace-only live driver
  (Statuspage optional, no-op LoggingPublisher). Mechanical tests stay green via a NEW recorded fixture
  from the live probe; AC6 internal verification is a manual PO-observed step. Single story, 5 pts."
branch: sprint-21
start_tag: sprint-21-start
# Capacity: velocity last-3 (sprints 18,19,20) = 5,5,5 -> mean 5.0. Committed 5 (STORY-016b).
#
# WORKFLOW: external implementation (2026-06-26) OR a Sonnet implementer if the PO asks. plan.md is the
# self-contained contract. Orchestrator runs the FULL six-command gate, the Opus spec + quality
# reviewers (5 pts), the mechanical wiki blast-radius sweep, then review/verdict/merge/retro. The PO
# triggers the back half with "do your review".
#
# PIPELINE: STORY-016b (5) -> GATE + Opus spec & quality reviewers (reviewers on Opus, 2026-06-24).
#
# DoD: NO new migration. NO new lint contract (executor/normalizer stay in adapters; LoggingPublisher is
# composition). lint-imports count stays 5.
#
# PROVENANCE: scope derived from a 2026-06-29 read-only live probe of the PO's tenant
# (https://bqm75769.apps.dynatrace.com, monitor HTTP_CHECK-DB5792CB88D14CF4 "TodoMVC Homepage Monitor").
# Dynatrace creds VERIFIED working; Statuspage key was rejected (401, wrong value) -> excluded this
# sprint. Real schema saved to memory `dynatrace-grail-real-schema`.
#
# Baseline at lock: branch sprint-21 cut from main @ 7ea535d (sprint-20 merged). Cheap gates green there.
# Agreements in force: composition/assembly-tests-build-real-objects (2026-06-29), clean-committed-tree
# (2026-06-29), merge-is-last-step (2026-06-29), mechanical wiki sweep (2026-06-28), named-domain-errors,
# empty+boundary tests (2026-06-25). See plan.md.
stories:
  - id: STORY-016b
    points: 5
    board: done            # todo | in-progress | blocked | review | done
    pipeline: gate+reviewers   # 5 pts -> impl (external) + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 1            # one fix loop (both Opus reviewers); PO authorized orchestrator inline fix.
    paused_at_commit: null
    dod_evidence:
      - command: "all SIX commands"
        exit_code: 0
        output_tail: "orchestrator-verified at 213034b (clean committed tree, post-fix): pytest 416
          passed (full suite minus the 2 dev_db container-harness meta-test files) + those 8 meta-tests
          pass clean in a churn-free env (= all green; the 3 transient failures were the orchestrator's
          own dev_db container churn during gate runs, not the code); lint-imports 5/0; fk 11/0; alembic
          up-to-date (no new migration); ruff check + format clean (project tree; the untracked .agents/
          harness dir excluded)."
        commit: 213034b
        at: 2026-06-29T09:30:00+05:30
    review:
      first_pass: "spec FAIL / quality REQUEST-CHANGES. Two blocking: (AC4, both reviewers)
        map_synthetic_status INVENTED failure mappings (code 1->DOWN, 2->DEGRADED + guessed message
        strings) against the plan's explicit 'do not invent failure values; fail-loud until observed
        live' — would mask the real failure code the AC6 live run is meant to capture, and untested;
        (AC1, spec) the build_dql_query unit tests were DELETED not updated, so nothing drove the new
        data object / filter field / injection guard / tz-rejection. Plus non-blocking minors (stale
        docstrings, local import, poll-budget ~3s, latency swallow, ns-timestamp edge tests). Structural
        agreements all HELD (composition test builds real objects, async poll genuinely driven, ns-parse
        correct, named errors present)."
      fix: "PO authorized orchestrator inline fix. AC4: stripped map_synthetic_status to HEALTHY/0->UP +
        UnknownVendorStatusError (fail-loud); the real DOWN/DEGRADED code is captured in AC6. AC1:
        restored 4 build_dql_query tests (real object, filter field, naive-watermark + breaking-char
        rejection). Consequence: two pull_loop failure tests relied on the removed invented mapping ->
        patched them to mock the vendor-mapping edge (production stays fail-loud). Folded-in minors:
        parse_ns_timestamp edge tests, stale docstrings, LoggingPublisher import hoist, poll budget
        (poll_interval_seconds=1.0 x 60 = 60s vs ~3s). Commits 24adb53 + 213034b."
      second_pass: "orchestrator re-verified: AC4 fail-loud confirmed; AC1 tests restored + green;
        all six gates green on the clean committed tree 213034b. AC1-AC5 MET; AC6 (live internal
        verification) is the manual PO step — pending."
      open_minors:
        - "clickpath_normalizer + map_execution_outcome remain on the legacy execution.outcome shape (out of live HTTP scope; retained, not in the live dispatch registry)."
        - "latency parse silently drops a non-numeric duration to None (latency is optional; reviewers said acceptable)."
    blast_radius_resolved: true   # compile pass @ 442b97d: 4 articles updated (dynatrace-adapter major
                                  # rewrite); sweep 0 stale / 0 missing / 0 bad links across 11.
hotfixes: []
interrupts: []
