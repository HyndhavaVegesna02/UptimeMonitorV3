# YourTeam active sprint board. No active sprint when status: none.
sprint: 63
status: active     # none | planning | active | review | closed | aborted
                   # LOCKED 2026-07-29 on PO approval ("start implementing sprint 63").
                   # SCOPE IS NOW FROZEN. New PO requests go to the backlog, not this sprint.
                   #
                   # One yt-plan-verifier pass ran pre-lock (the sprint is contract-sensitive:
                   # a vendor-path adapter and a safety-critical publish guard). Verdict GAPS:
                   # 9 findings, 6 blocking, ALL folded into plan.md before the PO saw it.
                   # Its sizing verdict drove the split of the old combined STORY-176 (6 pts by
                   # STORY-148's 3-point yardstick) into STORY-176 (this sprint, build) and
                   # STORY-182 (sprint 64, the run). See plan.md "Plan verification".
mode: in-process   # Standing directive after the sprint-60 external rejection: "you only
                   # implement". 180 (2 pt) and 181 (2 pt) -> implementer -> gate; 176 (3 pt) ->
                   # implementer -> spec | quality reviewers -> gate.
                   # Every story gets the DoD gate + a reality gate.
goal: "Build everything the demo run needs and make it SAFE TO ATTEMPT: a scripted scenario player, a >=12-component fictional fleet in STORY-146's nested config shape, a time base whose six constraints are each asserted, and a publish guard that holds even though real Statuspage credentials sit in the repo-root .env. Alongside it, close the two demo-engine defects that only start to matter once the engine is long-running, and retire sixteen stale code references no gate can see. THE RUN ITSELF IS STORY-182, SPRINT 64 — this sprint ends with the fleet authored, the player tested and the guard proven; nothing is launched. Backend + tools only; no frontend work."
branch: sprint-63
start_tag: sprint-63-start
start_commit: e107811   # sprint-62 tip. Branched from sprint-62, NOT from main: the PO accepted
                        # sprint 62 but kept it UNMERGED (standing "don't merge with main"), so
                        # main (517fc38) contains neither STORY-146's config shape nor STORY-148's
                        # engine — both of which STORY-176 depends_on. main and
                        # debug/ingest-stall-sample-mode (1257cc9) are two DIFFERENT branches;
                        # both are ancestors of HEAD, so nothing is lost by branching here.
started: "2026-07-29"
# -- Provenance ---------------------------------------------------------------
# Planning artefact: docs/scrum/sprints/2026-07-29-sprint-63/plan.md — contracts (15 STORY-176
# addresses re-verified at 95afc97, THREE corrected), per-story steps, per-story reality gates,
# and the full verifier record.
# Carried context: docs/scrum/sprints/2026-07-28-sprint-62/story-176-carried-to-sprint-63.md
# (its citations were confirmed at 57aa523 and STORY-146 moved code beneath them — use plan.md's
# corrected addresses, never the carry note's).
# -- Scope discipline ---------------------------------------------------------
# 7 points / 3 stories, at the ~9-11 baseline's lower edge deliberately: the PO's standing pacing
# directive ("do it multi sprint, with carefull verification, no need to rush in single stretch")
# and STORY-176 is the risk peak of the whole programme.
# EXPLICITLY OUT: the loop run itself (STORY-182, sprint 64); ALL frontend work (the design-system
# + shell sprint comes later and opens with a PO look-and-feel checkpoint); any failure-path
# scenario (STORY-177); STORY-147 (re-deferred 2026-07-29 to land with its consuming frontend);
# STORY-150/151/152/153 (breadth, rollup, completeness, rejection suppression); STORY-154 (BLOCKED
# on trial renewal); STORY-155 (sample_mode removal); STORY-173 (leaked-container fixture);
# STORY-178 (ANSI in the gate fragment); STORY-179 (dynamo_local ephemeral port).
# -- Live-data caveat ---------------------------------------------------------
# The Dynatrace trial expired 2026-07-28. No vendor FAILURE code has ever been observed, and
# map_synthetic_status RAISES on anything that is not "0"/"HEALTHY" (health_mapping.py:65) while
# dispatch.py:80 then discards the whole batch. The demo engine therefore emits UP + absence ONLY
# (PO decision D-A, sprint 62). NOTHING in this sprint may be reported as "the failure path is
# tested" — STORY-177 owns that.
# -- Safety precondition (non-negotiable) ------------------------------------
# decide DECIDES to publish recoveries with no human gate (decide.py:122-126) and PUBLISHES at
# decide.py:171-172, so any loop wired to the real publisher can post to the LIVE public
# Statuspage. NO LOOP IS STARTED IN THIS SPRINT AT ALL. That is precisely why STORY-176 ships the
# guard a sprint AHEAD of STORY-182's run: the guard's checks are exactly what must pass BEFORE a
# loop may start, and giving the run its own story makes that ordering impossible to reshuffle.
# The verifier found a BYPASS in the guard as first written — a demo component id colliding with
# a real one, on an API process running the DEFAULT CONFIG_DIR (which is what CLAUDE.md's own
# recipe step 4 does), PATCHes the real page. Closed by AC3(c), a mechanical disjointness check.
# -- Tooling friction (known, not blockers) ----------------------------------
# STORY-179: every DynamoDB-gated run needs DYNAMO_ENDPOINT_URL at a fixed-port container; record
#   it as an env_note on each gate record, as sprint 62 did.
# STORY-178: yt_gate.py's fragment carries ESC bytes from `npm run build` (invalid YAML). Strip
#   before merging and record the strip. Fourth sprint running.
# Retro amendment A1 BINDS: every reality_gate record needs discrimination_proof or two_sided_note.
# Retro amendment A2 BINDS: behavioural wiki Facts cite the test that pins them.
# -- Wiki state at lock -------------------------------------------------------
# yt_wiki.py at e107811: sweep 1 finding — dev-setup-and-dod.md STALE (.scrum/definition-of-done.md
# + CLAUDE.md moved under it). QUARANTINED: its claims may not enter any subagent brief this
# sprint until rehabilitated at the compile pass. facts CLEAN, links CLEAN, integrity CLEAN.
# Two code_ref amplifier notes (run.py in 4 articles, pyproject.toml in 5) — noted, not acted on.
# -- Baseline -----------------------------------------------------------------
baseline:
  head: e107811      # GREEN. The backend five re-run at the branch point BEFORE story 1 was
                     # dispatched, so a red inherited from the branch operation itself could not
                     # be mistaken for a story's doing. The three frontend commands were green at
                     # sprint 62's final_gate (2b0bf86) and no frontend file has changed since —
                     # `git diff --name-only 2b0bf86..e107811 -- frontend/` is docs-only
                     # (frontend/README.md).
  env_note: "DYNAMO_ENDPOINT_URL=http://127.0.0.1:8021 (fixed-port container uptime_dynamo_8021)
    to bypass the dynamo_local ephemeral-port defect (STORY-179). Environment-only; no repo file
    changed."
  dod_evidence:      # merged VERBATIM from yt_gate.py's emitted fragment — never hand-transcribed
    - command: "pytest"
      exit_code: 0
      output_tail: "tor.py ...                       [ 92%] | backend\\tests\\test_streak.py .........                                   [ 94%] | backend\\tests\\test_topology_domain.py ........                           [ 95%] | backend\\tests\\test_topology_endpoint.py ......                           [ 96%] | backend\\tests\\test_vendor_health.py ........                             [ 97%] | backend\\tests\\test_verdict.py ........                                   [ 99%] | backend\\tests\\test_zone_layout.py ....                                   [100%] | ============================ 572 passed in 40.38s ============================="
      commit: e107811
      at: "2026-07-29T16:11:23+00:00"
    - command: "python -c \"from importlinter.cli import lint_imports_command; lint_imports_command()\""
      exit_code: 0
      output_tail: "--------- | Contracts | --------- | Analyzed 150 files, 427 dependencies. | ------------------------------------- | core-independence KEPT | core-internal-layering KEPT | adapters-independence KEPT | api-feature-independence KEPT | api-outward-independence KEPT | adapters-edge-only KEPT | api-shared-no-feature-imports KEPT | src-no-tests KEPT | Contracts: 8 kept, 0 broken."
      commit: e107811
      at: "2026-07-29T16:12:06+00:00"
    - command: "ruff check ."
      exit_code: 0
      output_tail: "All checks passed!"
      commit: e107811
      at: "2026-07-29T16:12:07+00:00"
    - command: "ruff format --check ."
      exit_code: 0
      output_tail: "218 files already formatted"
      commit: e107811
      at: "2026-07-29T16:12:07+00:00"
    - command: "cfn-lint infra/stack.yaml"
      exit_code: 0
      output_tail: ""
      commit: e107811
      at: "2026-07-29T16:12:07+00:00"
stories:
  - id: STORY-180
    title: Demo-engine polish — the eight non-blocking minors from STORY-148's quality review
    file: docs/scrum/stories/STORY-180-demo-engine-polish.md
    status: todo
    points: 2
    ceremony: 1-2-pointer    # implementer -> DoD gate -> reality gate (no reviewer pair)
    order: 1                 # NOT high-risk-first, and the tradeoff is stated rather than hidden:
                             # 180 and 176 touch the same files (tools/demo_engine/store.py,
                             # server.py) and 180's fixes are ABOUT the long-running behaviour 176
                             # introduces, so doing them after means rework or a baked-in
                             # workaround. yt-plan-verifier agreed, on one condition it raised:
                             # AC2's route had to be DECIDED at planning (the two routes need
                             # OPPOSITE reality gates). Decided: equality test, not parse.
  - id: STORY-176
    title: "Grail demo engine part 2a — scenario player, demo fleet, time base, publish guard"
    file: docs/scrum/stories/STORY-176-demo-fleet-scenarios-and-loop-run.md
    status: todo
    points: 3                # was 4 combined; the run moved to STORY-182's 3
    ceremony: 3-pointer      # implementer -> spec | quality -> DoD gate -> reality gate
    order: 2                 # the risk peak: everything that must be true BEFORE a loop may start
  - id: STORY-181
    title: Retire stale references in code comments and docstrings (16 sites)
    file: docs/scrum/stories/STORY-181-retire-stale-code-references.md
    status: todo
    points: 2                # expanded pre-lock from 12 sites to 16 (verifier family (D))
    ceremony: 1-2-pointer    # implementer -> DoD gate -> reality gate
    order: 3                 # fully independent, comment-only, cannot block anything. Last on
                             # purpose. NOTE: .scrum/definition-of-done.md had the same rot and
                             # the orchestrator fixed it at planning — implementers may never
                             # write .scrum/ state.
story_gates: []
hotfixes: []
interrupts: []
