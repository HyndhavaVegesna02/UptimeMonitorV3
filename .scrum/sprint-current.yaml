# YourTeam active sprint board.
sprint: 25
status: closed        # none | active | review | closed | aborted
started: 2026-07-02
closed: 2026-07-02
goal: "Stand the frontend zone back up — second attempt, new design direction. Sprint 23-24's UI
  (DESIGN-airtable.md era) was fully reverted by the PO in 521764c; this sprint rebuilds the
  frontend shell (STORY-015a) guided by DESIGN-linear.app.md as a reference to think with, not
  copy: Vite+React+TS strict SPA, six-tab nav + routing, typed API client + MSW/Vitest harness,
  a theme-scoped token layer (dark = reference-native, light = derived; PO chose BOTH with a
  toggle), shell primitives (Button/StatusBadge/Panel/states), health tokens for both themes,
  and the three frontend DoD gates activated. Every later per-tab story (015b-015g) then just
  fills in one tab."
branch: sprint-25
start_tag: sprint-25-start
# NUMBERING: sprints 23/24 happened and were reverted (521764c); their branches preserve that
# history, so this sprint is 25 — the numbers 23/24 are burned, velocity.json simply has no
# entries for them.
#
# Capacity: velocity last-3 (sprints 20,21,22) = 5,5,3 -> mean 4.33. Committed 5 (STORY-015a) —
# a deliberate single-story focused sprint (same shape as sprint 22); the shell is the
# foundation everything else stacks on, so it gets the whole sprint.
#
# PO DECISIONS AT REFINEMENT (2026-07-02): (1) theme = dark + light with toggle; (2) working
# agreements start clean — the two reverted sprint-23/24 amendments were NOT re-adopted (their
# lessons are baked into story AC instead: AC1 scaffold pruning, AC6 non-text-only health
# color); (3) external implementation per the standing 2026-06-26 agreement; (4) split + shell-
# first approved. DESIGN-linear.app.md is a GUIDE (creative freedom), not a verbatim mandate —
# unlike the Airtable era.
#
# WORKFLOW (amended at lock, PO directive 2026-07-02): implementation is IN-PROCESS — a
# Sonnet 5 implementer subagent (model "sonnet", briefed for high effort) builds to plan.md
# as the self-contained contract, committing onto sprint-25. This supersedes the 2026-06-26
# external-handoff mode. The orchestrator then runs the FULL gate (six backend commands +
# the three new frontend commands), the Opus spec + quality reviewers (5 pts -> full
# pipeline), the mechanical wiki blast-radius sweep, then review/verdict/merge/retro.
#
# DoD: backend six commands UNCHANGED and must stay green (no backend source change in scope).
# Frontend gates (npm test / npm run build / npm run lint) become REAL in this story (AC7) —
# activated in .scrum/definition-of-done.md + CLAUDE.md in the same commit (command-sync).
#
# BASELINE at lock: branch sprint-25 cut from main @ 521764c. All six backend gates verified
# green there this session (2026-07-02): pytest 426 passed (throwaway postgres:16 via
# dev_db.py), lint-imports 5 kept / 0 broken, check_fk_direction 11 FKs / 0 violations,
# python -m alembic upgrade head exit 0, ruff check clean, ruff format --check 153 files clean.
# (Note: alembic runs via `python -m alembic` on this box — the alembic.exe shim is blocked by
# an Application Control policy.)
#
# Agreements in force: all of working-agreements.md as of 521764c (the sprint-23/24 frontend
# amendments were reverted and NOT re-adopted — PO chose start-clean at 2026-07-02 refinement).
stories:
  - id: STORY-015a
    points: 5
    board: done            # todo | in-progress | blocked | review | done
    pipeline: gate+reviewers   # 5 pts -> Sonnet 5 impl + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 1            # first-pass clean: both Opus reviewers passed, no fix loop
    paused_at_commit: null
    dod_evidence:
      - command: "all NINE commands (6 backend + 3 frontend)"
        exit_code: 0
        output_tail: "orchestrator-verified at 08d91e7 (clean committed tree). BACKEND: pytest 426
          passed (throwaway postgres:16 via dev_db.py); lint-imports 5 kept / 0 broken;
          check_fk_direction 11 FKs / 0 violations; `python -m alembic upgrade head` exit 0 (no new
          migration); ruff check 'All checks passed!'; ruff format --check '153 files already
          formatted'. FRONTEND (from frontend/): npm test 63 passed / 13 files / 0 skipped; npm run
          build (tsc -b && vite build) exit 0; npm run lint (eslint) exit 0. Note: alembic run via
          `python -m alembic` (the alembic.exe shim is blocked by an Application Control policy on
          this box)."
        commit: 08d91e7
        at: 2026-07-02T16:20:00+05:30
    review:
      first_pass: "spec PASS (all 7 AC MET) / quality APPROVE (0 Critical, 0 Major). Spec ran the
        three frontend gates itself, grep-verified no raw hex in components outside styles/, and
        COMPUTED WCAG ratios in both themes (every ink-as-text token >=4.5:1 on every surface;
        worst ink-subtle 5.14:1; ink-tertiary confirmed unused as text; accent is border/indicator
        only, never text). Confirmed AC2/AC3/AC5 tests DRIVE the named path (nav click switches the
        panel; error->retry asserts callCount==2; theme system-default/override/persist covered) —
        not name-matched. AC7 doc-sync + DoD activation are in the SAME commit (08d91e7). Quality
        confirmed the fetch hook is race-safe (cancelled-guard + attempt-keyed retry), the theme
        system is flash-free and consistent between inline script and React, the token layer is
        airtight, and the tests assert real behavior via accessible roles/text with MSW as the only
        I/O mock — nothing lies, nothing disabled (clean against the project's three prior
        'green-test-wrong-path' incidents). Backend diff empty; pyproject only the ruff exclude line.
        KEEP recommendation."
      fix: "None — both reviewers passed first-pass with zero blocking findings; no fix loop."
      second_pass: null
      open_minors:
        - "client.ts::getJson wraps fetch-rejection + non-2xx into ApiError, but a 2xx with a
          malformed body throws a raw SyntaxError from response.json() that escapes unwrapped
          (graceful — the probe catch handles any Error, no crash — but inconsistent error type on a
          path all six tabs copy). -> STORY-041."
        - "Button.tsx / Panel.tsx duplicate the `[...].filter(Boolean).join(' ')` classnames idiom;
          a one-line shared cx() helper before six tabs copy the primitives. -> STORY-041."
        - "mocks/handlers.ts is a single flat array + one fixture export — the shared append-point
          015b-015g all edit; per-feature handler modules would keep a tab story in its own files.
          -> STORY-041."
        - "AppShell.tsx has no catch-all/'*' route; an unknown path renders Nav + empty <main>
          (minor UX gap). -> STORY-041."
        - "statusMapping.ts unknown/partial_outage/major_outage branches not directly unit-tested
          (only operational/degraded via the probe); explicitly provisional code pending 015b."
        - "Sub-4px pixel literals (1px/2px/8px borders, badge dot) sit below the 4px spacing scale
          so cannot use a spacing token — consistent with the color-focused 'tokens, not hex' rule,
          noted only."
      ac_note: "No live/manual-only AC in this story (contrast to STORY-016b's AC6): all seven AC
        are mechanically verifiable and were verified in-session. No deferred/unexercised path."
    blast_radius_resolved: true   # compile pass @ branch HEAD 08d91e7: NEW frontend-zone.md article;
                                  # dev-setup-and-dod updated (frontend 3-command gate live + ruff
                                  # 'frontend' exclude + code_refs +frontend/package.json); api-five-file,
                                  # architecture-boundary, config-layer re-verified (pyproject 'frontend'
                                  # ruff-exclude only, Facts unaffected). Mechanical sweep: 0 stale /
                                  # 0 broken links across all 12 articles. verified_sha bumped to 08d91e7
                                  # (re-stamped to the merge SHA at close per edge-case #4).
hotfixes: []
interrupts: []
