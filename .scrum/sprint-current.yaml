# YourTeam active sprint board.
sprint: 3
status: active           # none | active | review | closed | aborted
goal: "Zone 2 closes — repository adapters implement the persistence ports against the spine, on a shared throwaway-DB harness so every later DB-gated story stops hand-rolling Postgres."
branch: sprint-3
start_tag: sprint-3-start
started: 2026-06-24
# Capacity: velocity history (8, 6, 6), mean ~7. Committed 6 (deliberate 1-pt under-commit).
# Green DoD baseline re-verified on main @ d5dd2c8 before lock: alembic upgrade head ->
# 3a8254bcfe59, pytest 77 passed, lint-imports 3 kept/0 broken, FK-direction 10 checked/0
# violations. (Note: the lint-imports.exe launcher was corrupted at verify time and
# regenerated via `pip install --force-reinstall --no-deps import-linter` — a .venv-only
# fix, no repo change.)
# Order: STORY-019 (DB harness) FIRST so STORY-007's integration tests are written against
# the shared fixture from the start (Sprint 2 retro sequencing call).
# Model assignment (PO rule): implementers -> Sonnet, reviewers -> Opus.
stories:
  - id: STORY-019
    points: 3
    board: in-progress     # todo | in-progress | blocked | review | done
    pipeline: full         # 3 pts -> implementer + spec reviewer + quality reviewer + DoD gate
    spec_review: PASS       # all 5 AC MET (verified by running). Non-blocking flag: teardown test
                            # flakes ~1/4 full-suite runs on concurrent container spin-up.
    quality_review: FIX_REQUIRED  # fix loop 1: 1 MAJOR — spawn-time container leak. If wait/migrate
                            # raises after start_container, the exception escapes resolve_db() before
                            # the fixture try/finally, so the finalizer never registers -> leaked,
                            # uniquely-named container. Bundling the spec reviewer's flakiness flag
                            # (same root cause) into this loop. 2 minors noted (non-blocking).
    blocked_question: null
    attempts: 1
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false  # touches backend/tests/conftest.py, test_spine_schema.py,
                                  # scripts/dev_db.py (new), CLAUDE.md. At DoD, match the diff
                                  # against dev-setup-and-dod.md code_refs (CLAUDE.md is one) and
                                  # migrations-and-db.md (the throwaway-DB one-liner) and
                                  # update/re-verify before Done.
  - id: STORY-007
    points: 3
    board: todo
    pipeline: full         # 3 pts -> implementer + spec reviewer + quality reviewer + DoD gate
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false  # touches backend/src/adapters/persistence/ (new repos) +
                                  # tests. canonical-types-and-ports.md (core/ports/) describes
                                  # the interfaces being implemented — not edited by an adapter,
                                  # but confirm no port signature drift; check at DoD.
hotfixes: []
interrupts: []
