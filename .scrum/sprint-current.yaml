# YourTeam active sprint board.
sprint: 10
status: review         # none | active | review | closed | aborted
started: 2026-06-27
goal: "Close out Zone 4 — the pipeline's final stage, decide, compares anti-flap's proposed status to
  the component's current published status and reconciles: worse -> a human-gate degradation proposal
  (no publish), better -> obsolete the open proposal + auto-publish a recovery, same -> nothing. Plus
  three cosmetic cleanups carried from recent retros/reviews (value-type coherence audit, a test-import
  tidy, dev_db.py idempotency)."
branch: sprint-10
start_tag: sprint-10-start
# Capacity: velocity history (...,6,6,6,6,6,6), last-3 mean (6,6,6) = 6. Committed 6.
# STORY-024 (decide, 3) is the headline — Zone 4 finale, unblocked now that STORY-028 (anti-flap),
# STORY-012 (proposals) and STORY-013 (publish) are all Done. Refined at sprint-10 planning: the open
# "current published status read" seam is resolved as components.status, INJECTED into decide as a
# `current_status: ComponentStatus` parameter (precedent: collapse's under_maintenance, anti_flap's
# thresholds); decide consumes the ProposalRepository + StatusPublisherPort ports it names; the
# components.status read wiring is deferred to STORY-016 (first end-to-end thread). decide is a core
# DecideService with injected ports (mirrors IngestService). Three 1-pt chores fill to 6.
# Order: STORY-024 first (highest blast radius, full review pipeline), then 029 (core-domain adjacent),
# 027 (test tidy), 030 (tooling).
#
# WORKFLOW (PO-directed 2026-06-26, working-agreements.md): the PO implements the plan externally
# (Antigravity / Gemini), committing onto this branch; the orchestrator does NOT dispatch implementer
# subagents. The PO triggers the review ("do your review"); the orchestrator then runs the FULL DoD
# gate + spec/quality reviewers (Opus, for the 3-pt story) + wiki blast radius + review/retro/merge.
# A story is Done only when all four DoD commands exit 0 AND (for 3+ pts) both reviews pass. Fix-loop
# findings route back to the PO/Gemini unless the PO asks the orchestrator to fix inline.
#
# Baseline at lock: ALL gates green on main @ 299925b — pytest 264 passed (with DB; Docker up),
# lint-imports 3 kept / 0 broken. No migration expected this sprint (decide is pure core; the chores
# touch tests + a script + possibly a core validator — no schema change).
# Model assignment: reviewers -> Opus (PO rule). Implementation -> PO/Gemini (external).
stories:
  - id: STORY-024
    points: 3
    board: done            # todo | in-progress | blocked | review | done
    pipeline: full         # 3 pts -> (external impl) + spec reviewer + quality reviewer + DoD gate
    spec_review: PASS       # Opus: all 3 AC MET — §10 direction (worse->create_open/no-publish,
                            # better->publish, same->nothing) + §12 reconciliation (supersede-vs-leave,
                            # recovered->obsoleted/nothing-published distinct from better->auto-publish),
                            # commit-first verified via FailingStatusPublisher. Tests exercise every branch.
    quality_review: APPROVE # Opus quality returned 1 MAJOR — decide.py shipped with NO docstrings
                            # (module/class/method), violating the core-service docstring convention
                            # (ingest_service.py/pipeline.py cite the dossier + explain the why). Fix loop 1
                            # (orchestrator, inline at PO request): added a module docstring (§10 decide +
                            # §12 reconciliation, the two comparisons, commit-first) + docstrings on
                            # DecideAction/DecideService/__init__/decide; logic untouched (ed87055). All
                            # other findings non-blocking minors (trailing blank lines, import style, a DRY
                            # nit in the two degradation branches, opened.id typing hole) -> STORY-031-style
                            # follow-up if the PO wants them.
    blocked_question: null
    attempts: 1             # one fix loop (orchestrator inline, docstrings) per PO choice
    paused_at_commit: null
    dod_evidence:
      - command: "pytest"
        exit_code: 0
        output_tail: "275 passed in 37.72s (orchestrator-verified, with DB @ ed87055)"
        commit: ed87055
        at: 2026-06-27T16:10:00+05:30
      - command: "lint-imports"
        exit_code: 0
        output_tail: "Contracts: 3 kept, 0 broken."
        commit: ed87055
        at: 2026-06-27T16:10:00+05:30
      - command: "python scripts/check_fk_direction.py  # throwaway postgres:16 via scripts/dev_db.py"
        exit_code: 0
        output_tail: "FK-direction OK: 10 foreign key(s) checked, 0 violations."
        commit: ed87055
        at: 2026-06-27T16:10:00+05:30
      - command: "alembic upgrade head  # same throwaway DB; no migration this sprint"
        exit_code: 0
        output_tail: "exit 0 (no-op upgrade)"
        commit: ed87055
        at: 2026-06-27T16:10:00+05:30
    blast_radius_resolved: true   # core-pipeline-and-availability.md (decide joins collapse/streak/
                                  # anti_flap; decide.py + test_decide.py in code_refs) + canonical-types-
                                  # and-ports.md (severity helpers in status.py). verified_sha to be
                                  # re-stamped to the merge commit (docstring commit ed87055 is no-Fact).
  - id: STORY-029
    points: 1
    board: done
    pipeline: gate         # 1 pt -> (external impl) + DoD gate only
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence:
      - command: "pytest / lint-imports / check_fk_direction / alembic upgrade head"
        exit_code: 0
        output_tail: "all four exit 0 at ed87055 (gate run over full tree). Audit of 12 frozen core
          types recorded in the story file; found + fixed the AvailabilityResult gap with a
          model_validator(mode=after) + tests for valid/invalid shapes."
        commit: ed87055
        at: 2026-06-27T16:10:00+05:30
    blast_radius_resolved: true   # AvailabilityResult validator -> core-pipeline-and-availability.md updated
  - id: STORY-027
    points: 1
    board: done
    pipeline: gate         # 1 pt -> (external impl) + DoD gate only
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence:
      - command: "pytest / lint-imports / check_fk_direction / alembic upgrade head"
        exit_code: 0
        output_tail: "all four exit 0 at ed87055. AvailabilityCalculator import hoisted to module top
          in test_availability.py; availability tests pass unchanged."
        commit: ed87055
        at: 2026-06-27T16:10:00+05:30
    blast_radius_resolved: true   # test-only; no src change -> no wiki impact
  - id: STORY-030
    points: 1
    board: done
    pipeline: gate         # 1 pt -> (external impl) + DoD gate only
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence:
      - command: "pytest / lint-imports / check_fk_direction / alembic upgrade head"
        exit_code: 0
        output_tail: "all four exit 0 at ed87055. dev_db.py up force-removes its own leftover container
          before docker run; integration test test_up_idempotent_against_leftover_container added."
        commit: ed87055
        at: 2026-06-27T16:10:00+05:30
    blast_radius_resolved: true   # scripts/dev_db.py -> dev-setup-and-dod.md re-verified
hotfixes: []
interrupts: []
