# YourTeam active sprint board.
sprint: 20
status: closed        # none | active | review | closed | aborted
started: 2026-06-29
closed: 2026-06-29
goal: "Make the first thread LIVE (dossier §8/§12/§17). Build the two real HTTP Executors (Dynatrace
  Grail DQL + Statuspage PATCH) left as injected seams, add their credentials to settings, carry the
  Statuspage component mapping in config, thread orchestration through run_periodic, and assemble a
  composition entrypoint that runs the live loop with BestEffortPublisher(RecordingPublisher(
  StatuspagePublisher)). Gate-green via recorded fixtures (no live call in any test); the live
  observation against the real SaaS is a manual post-merge smoke (plan.md T5), not a gated AC. Single
  story, 5 pts."
branch: sprint-20
start_tag: sprint-20-start
# Capacity: velocity last-3 (sprints 17,18,19) = 5,5,5 -> mean 5.0. Committed 5 (STORY-016).
#
# WORKFLOW: external implementation (2026-06-26) OR a Sonnet implementer if the PO asks (as in sprints
# 15/16/17/19). plan.md is the self-contained contract. The orchestrator runs the FULL six-command gate,
# the Opus spec + quality reviewers (5 pts), the mechanical wiki blast-radius sweep, then
# review/verdict/merge/retro. The PO triggers the back half with "do your review".
#
# PIPELINE: STORY-016 (5) -> GATE + Opus spec & quality reviewers (reviewers on Opus, 2026-06-24).
#
# DoD: NO new migration (statuspage_component_id is read-only config, never persisted). NO new lint
# contract (the executors live in adapters/ and may import httpx; composition/run.py imports both sides
# — allowed). lint-imports count stays 5.
#
# SCOPE NOTE (PO decision 2026-06-29): PO has BOTH Dynatrace + Statuspage creds and chose "build both
# Executors now". AC5 (live observation on the real SaaS) is a MANUAL post-merge smoke, not gate-verified
# — the pure-core/mockable-edges agreement holds, so every test uses recorded fixtures.
#
# Baseline at lock: branch sprint-20 cut from main @ 994b637. Topology prep committed @ 58b4072 (live
# httpcheck config, sockshop dropped, two real-config tests repointed; ruff clean, lint-imports 5/0,
# 404 passed there). Agreements in force: clean-committed-tree-gate (2026-06-29), merge-is-last-step
# (2026-06-29), mechanical wiki blast-radius sweep (2026-06-28), resource-lifecycle teardown-on-failure
# (2026-06-25), command-sync (2026-06-23), conventions checklist (2026-06-27). See plan.md.
stories:
  - id: STORY-016
    points: 5
    board: done            # todo | in-progress | blocked | review | done
    pipeline: gate+reviewers   # 5 pts -> impl (external) + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 1            # one fix loop: both Opus reviewers flagged the same blocking defect;
                          # PO authorized orchestrator inline fix (d9c2a77).
    paused_at_commit: null
    dod_evidence:
      - command: "all SIX commands"
        exit_code: 0
        output_tail: "orchestrator-verified at d9c2a77 (committed tree, post-fix): pytest 419 passed;
          lint-imports 5/0 (no new contract); fk 11/0; alembic up-to-date (no new migration —
          statuspage_component_id is read-only config); ruff check + format (153 files) clean."
        commit: d9c2a77
        at: 2026-06-29T07:30:00+05:30
    review:
      first_pass: "spec FAIL / quality REQUEST-CHANGES. Both Opus reviewers independently found the
        SAME blocking pair: run.py built the publisher chain with RecordingPublisher(publisher=...)/
        BestEffortPublisher(publisher=...) but the ctors take delegate= -> build_live_loop raised
        TypeError on startup (live loop could never run); and the assembly test
        (test_run_live_loop.py) over-mocked every constructor __init__, so the green gate was blind to
        it (the 'test that lies' pattern the spec-rigor agreements target). Quality also: MAJOR stray
        httpx2 in dev extra; MINOR CLAUDE.md missing the four live-secret env vars."
      fix: "PO authorized orchestrator inline fix (d9c2a77): run.py publisher= -> delegate=; assembly
        test rewritten to build the REAL objects (only run_periodic patched) and assert the
        BestEffort->Recording->Statuspage isinstance nesting + the six orchestration extras on each
        run_periodic call (now errors under the old wiring — guards the regression); dropped httpx2;
        documented the four live-loop secrets in CLAUDE.md."
      second_pass: "orchestrator re-verified: TypeError reproduced then closed; rewritten test passes
        and genuinely exercises the real ctors; all six gates green on the clean committed tree
        d9c2a77. AC1-AC4 MET; AC5 (live SaaS observation) is the manual post-merge smoke by design."
      open_minors:
        - "grail_executor/http_executor http_post/http_request seams typed as bare Callable (cosmetic; reviewers said leave)."
    blast_radius_resolved: true   # compile pass @ 0648cff: 8 articles updated/re-verified; sweep
                                  # 0 stale / 0 missing refs / 0 bad links across all 11.
hotfixes: []
interrupts: []
