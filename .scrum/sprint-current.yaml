# YourTeam active sprint board.
sprint: 18
status: active        # none | active | review | closed | aborted
started: 2026-06-29
goal: "Make Neon the seeded read model (dossier §7 Option C): add the signals.component_id
  migration, an idempotent seed_topology upsert (apps/components/signals; preserves runtime
  components.status), a scripts/seed_topology.py CLI, and app-startup (lifespan) seed wiring — so the
  spine is populated from config/apps and GET /api/v1/components shows real components. Single story,
  5 pts (top of range; boot wiring included per PO)."
branch: sprint-18
start_tag: sprint-18-start
# Capacity: velocity last-3 (sprints 15,16,17) = 5,5,5 -> mean 5.0. Committed 5 (STORY-040).
#
# WORKFLOW (working-agreements.md 2026-06-26): PO implements externally OR (quota permitting) the
# orchestrator finishes via a Sonnet implementer subagent (sprints 14-17). PO triggers the back half
# ("do your review"); orchestrator diffs sprint-18-start..HEAD, runs the FULL six-command gate, runs
# the Opus spec + quality reviewers (5 pts), resolves the wiki blast-radius (MECHANICAL sweep,
# 2026-06-28), then review/verdict/merge/retro. plan.md is the ONLY contract -> self-contained.
#
# PIPELINE: STORY-040 (5) -> GATE + Opus spec & quality reviewers (reviewers on Opus, 2026-06-24).
#
# DoD: ONE NEW MIGRATION this sprint (signals.component_id FK; spine->spine, so check_fk_direction
# stays green) -> `alembic upgrade head` does real work; the round-trip must reverse cleanly. NO new
# lint contract (seed is composition wiring). The 2026-06-28 TOCTOU agreement is N/A (upserts are
# ON CONFLICT DO UPDATE, not check-then-act).
#
# Baseline at lock: tree clean on main @ 9aed7b2 -> refined onto branch @ 44eaf43. Cheap gates green
# at lock (ruff clean; lint-imports 5 kept / 0 broken). DB gates green at sprint-17 review (b062132).
# Agreements in force incl. spec-review-verifies-AC-test-drives-behavior (2026-06-29) and the
# mechanical wiki blast-radius sweep (2026-06-28). See plan.md.
stories:
  - id: STORY-040
    points: 5
    board: todo            # todo | in-progress | blocked | review | done
    pipeline: gate+reviewers   # 5 pts -> (impl) + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false
hotfixes: []
interrupts: []
