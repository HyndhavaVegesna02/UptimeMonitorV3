# YourTeam active sprint board.
sprint: 5
status: active           # none | active | review | closed | aborted
started: 2026-06-25
goal: "Zone 3 ingest closes — the core ingest service + an asyncio pull loop pull from the
  Dynatrace adapter, validate-then-quarantine, dedupe on source_event_id, and advance the
  per-signal watermark over accepted observations only, committing before advancing so a
  crash mid-loop loses nothing."
branch: sprint-5
start_tag: sprint-5-start
# Capacity: velocity history (8, 6, 6, 6, 5), last-3 mean (6, 6, 5) = ~6. Committed 6.
# Order: STORY-009 FIRST (5 pts, the high-risk centerpiece — the §8 crash-safety ordering;
# a blocker there still leaves time for the 1-pt chore), then STORY-020 (1 pt mop-up in the
# adapter code touched last sprint). STORY-021 (reject native_id) is ready for Sprint 6.
# Green DoD baseline re-verified on main @ 8023941 before lock: pytest 109 passed,
# lint-imports 3 kept/0 broken, alembic upgrade head exit 0 (Dockerized postgres:16 via
# scripts/dev_db.py), check_fk_direction 10 checked/0 violations.
# Model assignment (PO rule, mandatory): implementers -> Sonnet, reviewers -> Opus.
stories:
  - id: STORY-009
    points: 5
    board: todo            # todo | in-progress | blocked | review | done
    pipeline: full         # 5 pts -> implementer + spec reviewer + quality reviewer + DoD gate
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false   # at DoD: this story ADDS a RejectedObservationRepository port
                                   # under core/ports/ (-> canonical-types-and-ports.md, code_refs
                                   # incl. core/ports/) and a Postgres rejected-obs adapter under
                                   # persistence/ (-> persistence-adapters.md). Both must be updated
                                   # or re-verified. dynatrace-adapter.md: the loop only CONSUMES
                                   # fetch_observations (no adapter change) -> re-verify only if the
                                   # diff touches the adapter dir.
  - id: STORY-020
    points: 1
    board: todo
    pipeline: light        # 1-2 pts -> implementer + DoD gate (no reviewers)
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false   # diff touches the dynatrace adapter (dispatch.py + _assembly.py)
                                   # -> re-verify dynatrace-adapter.md (code_refs incl. the package).
hotfixes: []
interrupts: []
