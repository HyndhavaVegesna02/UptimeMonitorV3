# YourTeam active sprint board.
sprint: 32
status: active        # none | active | review | closed | aborted
started: 2026-07-03
goal: "Make the sample switch operable and the availability story visible: the two-grain
  Availability tab (STORY-015d — component rollup + per-signal drill-down on the STORY-044
  endpoints, window presets, honest no-data treatment) and a Dashboard sample-switch toggle
  (STORY-049 — wired to the STORY-048 sample-mode API with an unmistakable ON warning)."
branch: sprint-32
start_tag: sprint-32-start
# Capacity: velocity last-3 (sprints 29,30,31) = 5,5,5 -> mean 5.0. Committed 5
# (STORY-015d 3 + STORY-049 2). PO approval 2026-07-03 (planning): "Approve and lock the
# recommended stories but run the backend while implementing the sprint so that i can view
# results" — approval of the recommended lock; the run-the-backend part is an OPERATIONAL
# request (the STORY-042 local stack — throwaway DB + uvicorn :8000 + live loop + Vite dev
# server — runs alongside implementation so the PO can watch live results), not sprint scope.
#
# WORKFLOW (in-process, PO directive 2026-07-02): Sonnet 5 implementer at HIGH effort builds
# to plan.md; STORY-015d (3 pts) gets Opus spec + quality reviewers (full pipeline);
# STORY-049 (2 pts) is implementer + DoD gate only. Frontend DoD surface: npm test /
# npm run build / npm run lint from frontend/, exit 0 on a clean committed tree. Backend is
# untouched this sprint — the six backend gates hold by the empty-diff baseline rule unless
# the diff shows a backend path touched (then a full six-gate re-run is required).
#
# TOOLING: none — pure frontend; Vitest + MSW + the per-tab pattern live since 015b/015c.
#
# CONSUMER-DTO CHECK (2026-07-02 agreement) done at planning against real backend models:
# topology GET /topology -> [{id,name,signals:[{signal_key,name,interval_seconds|null,
# component_id}]}]; availability GET /availability/component/{id}?since&until ->
# {component_id, rollup, signals[]} with availability_pct/completeness_pct float|null +
# verdict counts + window + computed_at (+ signal_key per child); sample-mode GET/PUT
# /sample-mode -> {enabled: bool}. No AC trimming needed.
#
# BASELINE at lock: sprint-32 cut from main @ 3785f12 — `git diff 61d786a..HEAD -- backend/
# scripts/ pyproject.toml alembic.ini migrations/ config/ frontend/` is EMPTY (docs/board/
# retro commits only since the sprint-31 gate-green state 61d786a), so the six-gate backend
# evidence recorded at sprint-31 close holds for this code state. frontend/ is unchanged
# since its last green three-gate run (sprint-27 close; sprints 28-31 were backend/docs-only).
#
# Agreements in force: all of working-agreements.md as of 3785f12 (incl. the 2026-07-03
# wiki-pass-commits-article-by-article rule; the 2026-07-02 tab-AC-vs-DTO check is DONE above;
# the AC-never-pre-declare-blast-radius rule — the mechanical sweep decides which articles
# update, but note sample-mode.md's REMOVAL inventory must gain STORY-049's frontend seam
# when the sweep flags it).
stories:
  - id: STORY-015d
    points: 3
    board: in-progress     # REOPENED 2026-07-03 ~20:00 — fix loop: percent-scale defect found by
                           # the orchestrator at review prep, demoing against the LIVE backend:
                           # availability_pct/completeness_pct are 0-1 FRACTIONS on the wire
                           # (live: 1.0 = 100%), but formatPct + the bar width consume them as
                           # percent-scale, and the MSW fixtures encoded percent-scale values
                           # (99.87) — so 146 green tests validated the wrong scale and the live
                           # tab shows '1.00%' for a fully-up component. ROOT CAUSE: the plan's
                           # "Verified API contracts" section pinned field types but NOT the
                           # scale, and its T4 example ('99.87%') implied percent-scale — a
                           # planning-precision miss, not an implementer invention. Retro input.
    pipeline: gate+reviewers   # 3 pts -> Sonnet 5 impl + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 2            # attempt 1 clean through reviewers; attempt 2 = focused scale fix
    paused_at_commit: null
    dod_evidence:
      - command: "npm test && npm run build && npm run lint (from frontend/, orchestrator-run on clean committed HEAD)"
        exit_code: 0
        output_tail: "npm test: 22 files / 125 tests passed (22.88s). npm run build: tsc -b && vite
          build OK (built in 232ms). npm run lint: eslint . clean. Backend untouched: diff
          9a42926..HEAD over backend/ scripts/ pyproject.toml alembic.ini migrations/ config/ is
          EMPTY — six-gate baseline (61d786a, sprint-31 close) holds."
        commit: 400331f
        at: 2026-07-03T19:32:00+05:30
    review:
      first_pass: "spec PASS (AC1-AC4 ALL MET; reviewer RAN the suite — 125/125 — and the 41
        AC-relevant tests, read test bodies: selector test asserts the ACTUAL tz-aware since/until
        MSW received incl. a genuinely-new refetch; null-pct asserts 'no data' + absence of 0%/NaN
        at BOTH grains; expand reveals name+key+pct children; no coverage deleted — additions only)
        / quality APPROVE (0 Critical, 0 Major; traced the refetch-identity chain
        useMemo[preset]->useCallback[range]->useFetch[fetcher] — stable, no loop; cancelled-guard
        covers the Promise.all fan-out so stale responses can't set state; useFetch genuinely
        untouched, both existing call sites unchanged; formatPct single formatter; tokens-only,
        bar aria-hidden with value always text; wiki update accurate, no overclaiming)."
      fix: "None — both reviewers passed first-pass with zero blocking findings."
      second_pass: null
      open_minors:
        - "Degenerate rollup renders literal '0/0 passing · 0 maint · 0 gap' next to 'no data' —
          honest but slightly redundant zeros; taste-only (quality reviewer, no change needed)."
      implementer_note: "Clean run, NO watchdog stall this sprint (contrast sprints 29/31) — the
        article-by-article wiki-commit agreement was honored (wiki commit 7e4fc68 separate).
        Design decisions: refetch via useCallback-fetcher identity (no useFetch contract change);
        zero-signal rows get no dead-end expand control; per-signal names joined from topology
        via signalNameByKey. Backlog candidates flagged: no live-clock window re-slide on a timer;
        bar color doesn't vary by availability threshold (accent-only)."
    blast_radius_resolved: true   # implementer updated frontend-zone.md (verified_sha c2e865c) as
                                  # its own commit 7e4fc68; swept all other articles — none
                                  # intersect frontend/; orchestrator will re-sweep at compile pass
  - id: STORY-049
    points: 2
    board: done            # awaiting PO verdict at review
    pipeline: gate             # 2 pts -> Sonnet 5 impl + DoD gate (no reviewers at this size)
    blocked_question: null
    attempts: 1            # clean single dispatch — no stall, no fix loop
    paused_at_commit: null
    dod_evidence:
      - command: "npm test && npm run build && npm run lint (from frontend/, orchestrator-run on clean committed HEAD)"
        exit_code: 0
        output_tail: "npm test: 23 files / 146 tests passed (14.43s; +21 over 015d's 125). npm run
          build: tsc -b && vite build OK (281ms). npm run lint: eslint . clean. Backend untouched:
          diff 37439dc..HEAD over backend/ scripts/ pyproject.toml alembic.ini migrations/ config/
          is EMPTY — six-gate baseline (61d786a, sprint-31 close) holds."
        commit: 1b9ef7d
        at: 2026-07-03T19:56:00+05:30
    review: null           # 2 pts -> gate-only per the default pipeline; no reviewer pass required
    implementer_note: "Clean run, no stall; wiki committed article-by-article (4e591dd, a06a85d)
      per the 2026-07-03 agreement. Design: single button role=switch with aria-checked; warning
      uses health-degraded (amber) token reserving red for real down-status; useSampleMode owns
      load+mutate in one hook (no list to reconcile, unlike Approvals) with render-derived
      enabled (override ?? loaded) after an effect-synced version showed a one-frame race.
      sample-mode.md REMOVAL inventory extended with every frontend file/seam incl. the shared
      putJson helper caveat. Backlog candidates: no distinct in-flight visual beyond disabled."
    blast_radius_resolved: true   # frontend-zone.md + sample-mode.md updated, verified_sha 63886bc
                                  # each its own commit; orchestrator re-sweeps at compile pass
hotfixes: []
interrupts: []
