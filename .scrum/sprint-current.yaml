# YourTeam active sprint board.
sprint: 9
status: active           # none | active | review | closed | aborted
started: 2026-06-26
goal: "Zone 5's two foundations land — the proposal substrate (StatusProposal types + state machine
  + ProposalRepository port + Postgres adapter with one-open-per-component) and the Statuspage
  publish adapter (StatusPublisherPort impl + best-effort no-rollback publish) — setting up decide
  (STORY-024) to tie proposals + publishing together next."
branch: sprint-9
start_tag: sprint-9-start
# Capacity: velocity history (8,6,6,6,5,6,6,6,6), last-3 mean (6,6,6) = 6. Committed 6.
# STORY-012 SPLIT at refinement to substrate-only (reconciliation rule -> STORY-024). STORY-013
# API surface resolved (component-status update). Both pure-ish Zone 5 foundations, both 3 pts,
# independent of each other.
# Order: STORY-012 first (proposal substrate), then STORY-013 (publish adapter).
#
# WORKFLOW CHANGE (PO-directed 2026-06-26, recorded in working-agreements.md): from this sprint on,
# the PO implements the plan externally (Antigravity / Gemini), committing onto this branch; the
# orchestrator does NOT dispatch implementer subagents. The PO triggers the review ("do your review");
# the orchestrator then runs the FULL DoD gate + spec/quality reviewers (Opus) + wiki blast radius +
# review/retro/merge. Mechanical floor unchanged: a story is Done only when all four DoD commands
# exit 0 AND both reviews pass. Fix-loop findings route back to the PO/Gemini unless the PO asks the
# orchestrator to fix inline.
#
# Baseline at lock: non-DB gates green on main @ e1ca578 (pytest 195 passed / 46 DB-gated skipped,
# lint-imports 3 kept/0 broken). DB-gated gates (alembic, check_fk_direction) were green at the
# sprint-8 merge with NO code/schema change since; could not be re-run at lock because Docker Desktop
# was down. No migration is expected this sprint (both stories use existing tables / no schema).
# Model assignment: reviewers -> Opus (PO rule). Implementation -> PO/Gemini (external).
stories:
  - id: STORY-012
    points: 3
    board: todo            # todo | in-progress | blocked | review | done
    pipeline: full         # 3 pts -> (external impl) + spec reviewer + quality reviewer + DoD gate
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false   # at review: adds StatusProposal/ProposalState (core/domain) +
                                   # ProposalRepository port (core/ports) -> canonical-types-and-ports.md;
                                   # PostgresProposalRepository (persistence) -> persistence-adapters.md.
  - id: STORY-013
    points: 3
    board: todo
    pipeline: full         # 3 pts -> (external impl) + spec reviewer + quality reviewer + DoD gate
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false   # at review: first adapters/outbound/ impl (statuspage) + a
                                   # composition best-effort publish helper. A NEW wiki article for the
                                   # publish path may be created at the compile pass (with its code_refs).
hotfixes: []
interrupts: []
