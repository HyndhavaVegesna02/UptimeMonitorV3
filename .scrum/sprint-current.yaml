# YourTeam active sprint board.
sprint: 2
status: active           # none | active | review | closed | aborted
goal: "Zone 2 spine exists — the eleven-table data model is migrated, reversible, and FK-direction-clean, so the repositories (Sprint 3) and every later zone have real tables to attach to."
branch: sprint-2
start_tag: sprint-2-start
started: 2026-06-24
# Capacity: velocity history = 2 sprints (8 accepted, 6 accepted), mean ~7. Committed 6
# (deliberate 1-pt under-commit behind one load-bearing foundation story). Green DoD
# baseline re-verified on main @ 8374de1 before lock: pytest 45 passed, lint-imports 3 kept
# 0 broken, alembic upgrade head -> eda70ac11454, FK-direction 0 violations.
# Plan decision (refinement): STORY-006 builds the FULL 11-table spine; STORY-007 (repos)
# was sequenced into Sprint 3 so the spine is PO-accepted before adapters build on it.
# Model assignment (PO rule): implementers -> Sonnet, reviewers -> Opus.
stories:
  - id: STORY-006
    points: 5
    board: in-progress     # todo | in-progress | blocked | review | done
    pipeline: full         # 5 pts -> implementer + spec reviewer + quality reviewer + DoD gate
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 1
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false  # high blast radius: the schema foundation. The forward
                                  # blast-radius check at DoD must match the migration's new
                                  # tables against architecture-boundary.md code_refs and any
                                  # §9 schema article, updating/re-verifying before Done.
  - id: STORY-018
    points: 1
    board: todo
    pipeline: lite          # 1 pt -> implementer + DoD gate (no reviewers)
    spec_review: null
    quality_review: null
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence: []
    blast_radius_resolved: false  # non-functional repo-hygiene change; no code_refs expected
hotfixes: []
interrupts: []
