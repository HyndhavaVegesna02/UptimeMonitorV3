# YourTeam active sprint board.
sprint: 37
status: active        # none | active | review | closed | aborted
started: 2026-07-06
goal: "Close the two remaining console-free maintenance gaps: the maintenance ordering
  violation returns a clean, form-mappable 422 instead of a leaked Pydantic blob
  (STORY-052), and the Dashboard visibly marks components that are under maintenance right
  now (STORY-046)."
branch: sprint-37
start_tag: sprint-37-start
# Capacity: recorded velocity last-3 = sprint 33 (5), 34 (5), 36 (6) -> mean 5.33. Sprint 35
# is PARKED/unmerged (no accepted points recorded), so it is not in the mean. Committed 3
# (052=1 + 046=2) — a DELIBERATE under-commit: these are the ONLY two console-free `ready`
# stories. STORY-017 (deploy, parked on sprint-35) and STORY-053 (failure-shim proxy) both
# need PO console time, which the PO deferred; padding with a console-gated story would
# block mid-sprint. PO approval 2026-07-06 (planning): "Approve & lock" as proposed.
#
# PARKED SPRINT NOTE (unchanged): sprint 35 (STORY-017 deployment) is PAUSED at the PO's
# request (2026-07-06); its code lives UNMERGED on branch sprint-35, frozen board snapshot
# at docs/scrum/sprints/2026-07-06-sprint-35/paused-board.yaml. Resume recipe: after a
# later sprint merges, rebase sprint-35 onto main (expect conflicts in pyproject.toml,
# CLAUDE.md, .scrum files — keep BOTH stories' content), re-run the nine gates, restore the
# board from the snapshot, then continue at STORY-017's live tail (GitHub repo = step 0).
# STORY-017 stays sprint: 35 in the backlog; sprint-37 does NOT touch it.
# NOTE: sprint-37 changes frontend maintenance files (fieldError.ts, DashboardPage) and
# backend maintenance/validation.py — none overlap STORY-017's CORS/deploy-config diff, so
# the eventual sprint-35 rebase gains no NEW conflict from sprint-37 beyond the usual
# .scrum/CLAUDE.md/pyproject bookkeeping.
#
# WORKFLOW: both stories are <=2 pts -> gate-only pipeline (Sonnet 5 implementer at HIGH
# effort + DoD gate; NO spec/quality reviewer pipeline, per the size rule). STORY-052
# touches BOTH backend + frontend -> full six-gate backend DoD + three-gate frontend DoD.
# STORY-046 is frontend-only -> three-gate frontend DoD; backend gates still run and must
# show an EMPTY backend diff (frontend/ change only).
# DB-gated pytest runs against an ISOLATED throwaway DB (unset DATABASE_URL so the fixture
# spawns its own container) while the PO's local stack keeps running on 55432 — the
# 2026-07-02 single-non-concurrent-invocation agreement applies.
#
# CONSUMER-DTO CHECK (2026-07-06, at planning): STORY-046 renders active-maintenance state
# from GET /api/v1/maintenance. Verified against api/v1/maintenance/models.py::
# MaintenanceWindowDTO = {id, component_id, starts_at, ends_at, reason} — tz-aware ISO
# datetimes, NO server-side `state` field; active/upcoming/past is a CLIENT derivation the
# frontend already does (features/maintenance/windowState.ts::deriveWindowState, half-open
# starts_at <= now < ends_at). All fields the AC needs exist on the wire; 046 reuses that
# derivation keyed by component_id. No producer gap; no AC trim needed.
# STORY-052's failure-path claim was already live-probed (sprint-34): the domain layer
# rejects end-before-start AND equal timestamps (422); 052 only relocates the message to
# the edge validator for a clean one-liner — no new producer capability claimed.
#
# TOOLING: none.
#
# BASELINE at lock: sprint-37 cut from main @ b64a122 (tag sprint-37-start). Everything on
# main since the last green gate (8237962) is docs/.scrum only — the code diff
# 8237962..b64a122 over backend/ frontend/ scripts/ config/ pyproject.toml alembic.ini
# migrations/ is EMPTY (verified at lock) — so the sprint-36 gate evidence holds for the
# cut point: backend 511 passed @ 8237962 lineage + frontend 222 passed @ 53ad5a1 lineage.
#
# ORDER: 052 (1) -> 046 (2). 052 first: higher blast radius (backend maintenance/
# validation.py + wiki articles), a crisply-specified defect that lands an early Done and
# warms the maintenance-frontend context (fieldError.ts, MaintenancePage) that 046 reuses;
# 046 second reuses that context + the windowState derivation, frontend-only, slightly more
# open-ended UI, so a slip there has the least blast radius.
stories:
  - id: STORY-052
    points: 1
    board: done            # awaiting PO verdict at review
    pipeline: gate             # 1 pt -> gate-only (no reviewers)
    blocked_question: null
    attempts: 1            # implementer (Sonnet 5) committed all 7 tasks clean (c2c6651..127c8fb)
                           # then was interrupted when the previous process exited — its final
                           # report (DoD evidence) was LOST. Orchestrator verified per the
                           # 2026-06-25 crash-recovery rule: coherent committed work, clean tree,
                           # no leaked artifacts; then RAN ALL NINE GATES + the wiki sweep itself
                           # to establish the evidence below (verification-before-completion).
    paused_at_commit: null
    dod_evidence:
      - command: "pytest (isolated throwaway DB via dev_db.py on 55432; PO stack not running) + lint-imports + check_fk_direction + alembic upgrade head + ruff check + ruff format --check; frontend npm test/build/lint — ALL orchestrator-run on clean committed HEAD after the implementer interruption"
        exit_code: 0
        output_tail: "Backend @ 127c8fb: 513 passed (+2 over sprint-36's 511: end-before-start +
          equal-timestamp clean-detail ordering-422 tests); 5 contracts kept / 0 broken; FK 11/0;
          alembic OK; ruff check + format both clean. Frontend @ 127c8fb: 33 files / 225 passed
          (+3 over 222: fieldError ends_at-mapping + multi-field determinism + MaintenancePage MSW
          inline-render); build OK (257.86 kB js); lint clean."
        commit: 127c8fb
        at: 2026-07-07T12:05:00+05:30
    review: null           # 1 pt -> gate-only per the default pipeline
    implementer_note: "Backend fix is exactly the ordering check in maintenance/validation.py
      (ends_at <= starts_at -> SyntacticValidationError 'ends_at must be strictly greater than
      starts_at.', firing in service.py step 1 BEFORE domain construction, so the clean one-liner
      replaces the raw Pydantic blob); service.py UNCHANGED; domain validator kept as defense in
      depth. Frontend: fieldError.ts checks the 'strictly greater than' phrase FIRST -> ends_at
      (before the generic substring scan, which would else mis-map to starts_at/component_id), doc
      comment rewritten to the THREE 422 cases; both sprint-34 minors folded in (stale doc + the
      multi-field-determinism test). MaintenancePage.tsx source UNCHANGED — the page already
      consumed fieldErrorFromDetail; only its test was added."
    blast_radius_resolved: true   # orchestrator re-ran the mechanical staleness sweep over ALL
                                  # docs/scrum/wiki/*.md @ 127c8fb -> ALL CURRENT (implementer had
                                  # swept api-five-file-convention @ 240666e + frontend-zone @ 127c8fb)
  - id: STORY-046
    points: 2
    board: done            # awaiting PO verdict at review
    pipeline: gate             # 2 pts -> gate-only (no reviewers)
    blocked_question: null
    attempts: 1            # clean single dispatch (Sonnet 5) 7e0f4fd..a518c45; orchestrator
                           # ran the gate verification (npm test contention false-red handled
                           # per the 2026-07-02 precedent — see dod_evidence)
    paused_at_commit: null
    dod_evidence:
      - command: "frontend three-gate DoD (npm test / build / lint) orchestrator-run on clean HEAD; backend six-gate carried by EMPTY backend diff 127c8fb..HEAD"
        exit_code: 0
        output_tail: "Frontend @ a518c45: VALID signal = 230 passed / 34 files (npx vitest run
          --no-file-parallelism, exit 0) — the +8 over sprint-36's 222 (STORY-052 +3 & STORY-046
          +5); build OK (tsc -b && vite build, exit 0); lint clean (eslint, exit 0). NOTE: the
          canonical `npm test` (default file parallelism) INTERMITTENTLY exits 1 on ONE
          PRE-EXISTING, UNRELATED test — CheckHistoryPage's 1500-row 1000-cap render hits Vitest's
          5000ms default timeout under CPU contention (empty diff since sprint-37-start; passes in
          ISOLATION in 3.6s / 11 passed; passed green during STORY-052's own npm test run). Treated
          as a contention-induced FALSE-RED per the binding 2026-07-02 agreement (re-run cleanly for
          the real signal); filed as defect STORY-054 so the gate itself becomes deterministic.
          Backend six gates unchanged since 127c8fb (513 passed / 5 contracts / FK 11-0 / alembic OK
          / ruff clean) — backend diff 127c8fb..HEAD is empty."
        commit: a518c45
        at: 2026-07-07T12:40:00+05:30
    review: null           # 2 pts -> gate-only per the default pipeline
    implementer_note: "Frontend-only (this session's backend diff empty). New thin hook
      features/dashboard/useMaintenanceWindows.ts = useFetch(getMaintenance) mirroring
      useComponents; DashboardPage computes per-component isUnderActiveMaintenance() by filtering
      windows on component_id and OR-ing deriveWindowState(...)=='active' (reuses the existing
      half-open windowState.ts; boundary at starts_at active / ends_at not). The indicator is a
      SECOND StatusBadge (status='maintenance', label='Under maintenance') in a .dashboard-status-cell
      flex wrapper ALONGSIDE the health badge — never replacing it (dossier §6/§11), never color-only.
      statusMapping.ts / ComponentStatus UNTOUCHED. Graceful degradation: any non-'success'
      maintenance fetch state -> [] -> no marker (dedicated 500-response test proves the components
      table still renders). AC1/AC2/AC3 all covered by MSW tests."
    open_minors:
      - "The canonical `npm test` is flaky on this machine (pre-existing CheckHistoryPage timeout
        under parallelism) -> STORY-054 filed; sprint-37 retro input."
    blast_radius_resolved: true   # implementer swept frontend-zone (@ 9f16880) + sample-mode
                                  # (@ a518c45); orchestrator re-runs the full sweep in the
                                  # sprint-close compile pass below.
hotfixes: []
interrupts: []
