# YourTeam active sprint board
sprint: 59
status: active
mode: in-process      # PO-approved at lock (2026-07-21).
goal: "Stand up the new greenfield frontend on the approved refimg-derived visual language: a documented design system + Phosphor icons (STORY-120), the re-skinned collapsible app shell (STORY-121), and the full Dashboard live on real backend data (STORY-122). One complete, demoable vertical slice = the approved prototype, running."
branch: sprint-59
start_tag: sprint-59-start
start_commit: 280cac2
started: "2026-07-21T00:00:00+05:30"
# PO decisions (2026-07-21): greenfield-then-swap (rebuild frontend/src fresh, keep only build
# toolchain; PO acceptance at review IS the swap); scope = design system + shell + Dashboard;
# light-first with dark-ready tokens; collapsible desktop sidebar REQUIRED; animations are a
# first-class deliverable (STORY-121 bumped 2->3 pts, gets reviewer pair). Reference = the
# approved prototype (docs/scrum/sprints/2026-07-18-ui-prototyping/prototypes/refimg-dashboard.html)
# — adapt with craft, not a rigid pixel spec.
# NON-NEGOTIABLE: frontend only. No backend/config/infra/API/DynamoDB/pipeline edits. No new
# Docker resources. New API client re-derived from the LIVE /api/v1 contracts (verified at the
# reality gate); old types.ts read only as a contract reference.
# Mandatory skills in every implementer + reviewer brief: ui-ux-pro-max, web-design-guidelines,
# vercel-react-best-practices, emil-design-eng, design-system.
# Icons: Phosphor (@phosphor-icons/react) behind an accessible Icon wrapper.
# Plan-verifier: SKIPPED (token economy, PO-approved 2026-07-15) — not contract-sensitive: UI
# rendering against already-live /api/v1 contracts; no adapter/vendor path, no units/scale
# logic; in-process. Per-story reality gate (live render-vs-wire) covers contract consumption.
# Numbering: sprint 59 (52-58 were the rejected rewrite on abandoned branches); stories use a
# clean 120-122 block to avoid collision with the frozen rewrite's 097-110.
# Lock: this session took the lock from the prior codex-...ui-rewrite-codex holder (separate
# branch/initiative) per PO direction to run this work here on the ui-prototype line.
# STARTED 2026-07-21 (PO: "start the sprint"). Baseline gate GREEN on HEAD 6a4db7c — all 8
# commands exit 0 (pytest 529 passed, import-linter 8/8, ruff check+format, cfn-lint, npm
# test 363 passed, npm build, npm lint). First baseline run showed 51 pytest ERRORS in
# DB-gated tests: a stale `uptime_dynamo` container (Exited 255, 36h old) held port 8001 and
# broke the session-scoped dynamo_local fixture; removed it and pytest went green (529). Empty
# code diff since sprint cut 280cac2 (docs-only) + passes in isolation → transient, discounted
# per agreement 2026-07-06. FOR RETRO: consider a preflight that reaps stale uptime_dynamo* containers.
# Tooling: ui-ux-pro-max IS installed at .agents/skills/ui-ux-pro-max (not .claude/skills, whose
# copy is stale bytecode) — invoke `python .agents/skills/ui-ux-pro-max/scripts/search.py`. PO
# confirmed 2026-07-21. All five mandated skills available.
stories:
  - id: STORY-120
    title: Design-system foundation + Phosphor icons
    status: in_progress
    points: 3
    ceremony: 3-pointer   # implementer -> spec || quality -> scoped DoD -> reality gate
  - id: STORY-121
    title: App shell re-skin — collapsible sidebar + topbar, responsive, first-class motion
    status: todo
    points: 3
    ceremony: 3-pointer   # bumped from 2 at lock (PO added collapsible sidebar + motion scope)
  - id: STORY-122
    title: Dashboard page on real backend data
    status: todo
    points: 3
    ceremony: 3-pointer
