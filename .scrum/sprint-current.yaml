# YourTeam active sprint board.
sprint: 29
status: active        # none | active | review | closed | aborted
started: 2026-07-02
closed: null
goal: "Close the product's core-loop dead end (STORY-045): an approved degradation actually
  reaches Statuspage through the Recording->BestEffort chain, is recorded in `publications`,
  and writes `components.status` back — so the Dashboard reflects reality, the next decide
  cycle compares against the truth (no approval whack-a-mole), and the recovery auto-publish
  branch becomes reachable for the first time."
branch: sprint-29
start_tag: sprint-29-start
# Capacity: velocity last-3 (sprints 26,27,28) = 5,5,3 -> mean 4.3. Committed 5 (STORY-045) —
# the 28 entry was a deliberate under-commit, not a miss; sprints 25-27 each delivered 5/5 and
# the PO approved the single-5 commitment at planning ("Start with story 45", 2026-07-02).
#
# WORKFLOW (in-process, PO directive 2026-07-02): Sonnet 5 implementer at HIGH effort builds to
# plan.md; Opus spec & quality reviewers (5 pts -> full pipeline); orchestrator runs the full
# six-command backend DoD gate as a SINGLE non-concurrent DB-gated run (2026-07-02 agreement),
# wiki blast-radius sweep, then review/verdict/merge/retro.
#
# TOOLING: none — pure backend against existing infra. No frontend change.
#
# DoD: the six backend commands must stay green. AC4's write-back-vs-publish ordering is PINNED
# in plan.md (commit-first: DB writes durable BEFORE the best-effort external call) per the
# 2026-06-26 plan-edge-behavior agreement. No live-credential AC — the whole story is provable
# with fakes + the throwaway DB, so nothing is carved out as manual.
#
# BASELINE at lock: sprint-29 cut from main @ 162783d (docs + .gitignore housekeeping only since
# the sprint-28 gate-green code state 6303247 — `git diff 6303247..HEAD -- backend/ scripts/
# pyproject.toml alembic.ini migrations/ config/ frontend/` is EMPTY, so the six-gate evidence
# recorded at sprint-28 close holds for this code state).
#
# Agreements in force: all of working-agreements.md as of 162783d (incl. the 2026-07-02
# single-non-concurrent-DB-gate rule and the Sonnet-5 in-process directive; the tab-AC-vs-DTO
# agreement is N/A — no consumer-tab AC this sprint).
stories:
  - id: STORY-045
    points: 5
    board: in-progress     # todo | in-progress | blocked | review | done
    pipeline: gate+reviewers   # 5 pts -> Sonnet 5 impl + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 1            # implementer stalled after T4 commit + finished (uncommitted) wiki edits;
                           # coherent tail recovered + committed by the orchestrator (2026-06-25
                           # crash-recovery agreement) — no fix loop yet, reviewers pending
    paused_at_commit: null
    dod_evidence:
      - command: "all SIX backend commands (orchestrator-run on committed HEAD, single non-concurrent DB-gated sequence)"
        exit_code: 0
        output_tail: "gate at 6d1799a (clean committed tree). ruff check 'All checks passed!'; ruff
          format --check '156 files already formatted'; lint-imports 5 kept / 0 broken (120 files,
          345 deps); pytest 444 passed in 113.66s (SINGLE run vs throwaway postgres:16 via
          dev_db.py; +16 tests over sprint-28's 428); check_fk_direction 11 FKs / 0 violations;
          alembic upgrade head exit 0. Container torn down after."
        commit: 6d1799a
        at: 2026-07-03T00:05:00+05:30
    review: null
    blast_radius_resolved: true   # implementer updated 5 articles (statuspage-publish,
                                  # canonical-types-and-ports, persistence-adapters,
                                  # core-pipeline-and-availability, api-five-file-convention) to
                                  # verified_sha 7cabee7; orchestrator re-runs the mechanical sweep
                                  # at the compile pass before review
hotfixes: []
interrupts: []
