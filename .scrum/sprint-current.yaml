# YourTeam active sprint board
sprint: 60
status: active   # PLANNED + LOCKED 2026-07-22. Branched from the sprint-59 tip (106bdb7,
                 # new-frontend/ui-prototype line, unmerged). Plan-verifier LOCK_READY (zero gaps).
                 # MODE PIVOT 2026-07-22: an external agent delivered all 5 tabs (commits f37f82d..
                 # b100a1a) but the PO REJECTED the work ("didn't like it"). Those 5 commits were
                 # reset off the branch (HEAD back to planning commit 947b0d1) and preserved at tag
                 # scrap/sprint-60-external-rejected. PO directed in-process re-implementation
                 # ("you only implement"). Same scope, same plan.md spec.
mode: in-process # PIVOTED from external 2026-07-22 (PO rejected the external delivery). Orchestrator
                 # dispatches yt-implementer per story (TDD, commit per green step); 3+ pt stories get
                 # spec ∥ quality reviewers; all get DoD gate + reality gate. plan.md remains the build
                 # spec (contracts verified LOCK_READY); PO directive: design FRESH with the mandated
                 # skills — do NOT reuse the rejected external work.
goal: "Complete the operator cockpit begun in sprint 59: build the 5 remaining tabs — Availability, Check History, Approvals, Maintenance, Publications — as fresh high-craft designs on the sprint-59 design system + shell, each live on the real /api/v1 backend. At sprint end all six routes are real; no placeholders remain."
branch: sprint-60
start_tag: sprint-60-start
start_commit: 106bdb7   # the sprint-59 tip this branch was cut from (planning commit sits on top)
started: "2026-07-22T00:00:00+05:30"
# PO decisions (2026-07-21):
#   - Scope = ALL 5 remaining placeholder tabs in one sprint (PO chose "all 5" over the
#     2-3-tab options; 20 pts, well above ~9 velocity — owned; Publications is the natural
#     carryover if the review tail runs long).
#   - DESIGN FRESH: do NOT copy the old tab UI shapes — "be creative and design better ones
#     with skills." AC pin capabilities + data-binding + states + a11y + motion + responsive;
#     layout/visual craft is the builder's.
# NON-NEGOTIABLE: frontend only. No backend/config/infra/API/DynamoDB/pipeline edits, no new
#   Docker resources. API client re-derived/extended from the LIVE /api/v1 contracts (the
#   sprint-59 client is GET-only + partial; plan.md §Shared-foundation lists what each page adds).
# Mandatory skills in every implementer + reviewer brief: ui-ux-pro-max (live copy at
#   .agents/skills/ui-ux-pro-max), web-design-guidelines, vercel-react-best-practices,
#   emil-design-eng, design-system.
# Plan-verifier: DISPATCHED + PASS (external mode = plan.md is the full contract; contract-sensitive
#   by rule). Verdict LOCK_READY, zero gaps (2026-07-22). Two non-blocking observations folded into
#   plan.md §Availability (rollup distinct_locations=0 quirk; interval_seconds nullable).
# External delivery contract (execution-modes.md §2): commit-per-story with STORY-NNN: messages on
#   sprint-60 (ideally per-green TDD cadence); if work returns as one uncommitted tree the
#   orchestrator commits it per-story BEFORE reviewing; a self-reported "gates green" is a to-verify
#   list, never evidence — only the orchestrator's own yt_gate.py on the final HEAD counts.
# Gates: mid-sprint scoped `yt_gate.py --only npm`; sprint close = full 8-command gate on final HEAD
#   (DYNAMO_ENDPOINT_URL unset; reap stale uptime_dynamo* containers first). Reality gate per story
#   against the live stack (already UP: DynamoDB :8001 + API :8000 + loop; Vite :5173 at gate time).
stories:
  - id: STORY-129
    title: Availability page — two-grain (rollup + per-signal drill-down), window toggle
    status: done
    points: 5
    ceremony: 3-pointer   # in-process: implementer → spec ∥ quality → DoD gate → reality gate
    order: 1
    reviews:
      spec: "PASS — all 7 AC MET with genuine AC-to-test traces (×100 scaling + null→'No data' explicitly tested; AC5 independence proven with a delayed MSW fetch; window toggle asserts trailing-Z bounds). 454 tests."
      quality: "APPROVE — 0 critical / 0 major. Truthful rendering (rollup distinct_locations=0 quirk rendered honestly, signal child=2); fixtures byte-match the plan's live samples; no tests-that-lie. 3 minors → STORY-134 (flaky delay-based AC5 test hardening; a few px spacing literals)."
    reality_gate: "PASS (live stack, scripted Chromium). Rollup availability 100.00% + completeness 17.71% (fraction×100 exact vs GET /availability/component/http-check), 126/126 verdicts, 0 down, 594 gap, Locations 0 (rollup quirk honest); drill-down reveals real http-check signal with Locations 2; 7d toggle refetches; exactly one h1; no horizontal scroll @390+1440; zero console errors/warnings. My Vite (5174) torn down + port freed; user's 5173 dev server left up."
    dod_evidence:   # scoped --only npm at HEAD 2280f67
      - command: "npm test"
        exit_code: 0
        output_tail: "Test Files  59 passed (59) | Tests  454 passed (454)"
        commit: 2280f67
      - command: "npm run build"
        exit_code: 0
        output_tail: "built (index js 335.13 kB / gzip 100.22 kB)"
        commit: 2280f67
      - command: "npm run lint"
        exit_code: 0
        output_tail: "eslint . — clean (no output)"
        commit: 2280f67
  - id: STORY-130
    title: Check History page — multi-signal merge, filter toolbar, dense grid
    status: todo
    points: 3
    ceremony: 3-pointer
    order: 2
  - id: STORY-131
    title: Approvals page — pending proposals + approve/reject confirm flow (first mutating page)
    status: todo
    points: 5
    ceremony: 3-pointer
    order: 3
  - id: STORY-132
    title: Maintenance page — windows list + schedule form + delete (422 field mapping, UTC)
    status: todo
    points: 5
    ceremony: 3-pointer
    order: 4
  - id: STORY-133
    title: Publications page — Statuspage publish-attempt timeline (outcome + author)
    status: todo
    points: 2
    ceremony: 1-2 pointer   # in-process: implementer -> DoD gate -> reality gate (no reviewer pair at 2 pts)
    order: 5
