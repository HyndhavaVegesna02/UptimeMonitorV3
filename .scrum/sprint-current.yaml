# YourTeam active sprint board.
sprint: 14
status: closed        # none | active | review | closed | aborted
closed: 2026-06-28
started: 2026-06-28
goal: "Stand up the Maintenance feature module — a MaintenanceRepository over the EXISTING
  maintenance_windows table + a functional Maintenance tab (GET list + POST schedule at
  /api/v1/maintenance) — and harden the import floor with the 5th src->tests linter contract.
  Order: STORY-038 (1) FIRST (so its new contract immediately guards STORY-036's new code), then
  STORY-036 (5). Committed 6 pts (= the 6.0 last-3 mean)."
branch: sprint-14
start_tag: sprint-14-start
# Capacity: velocity last-3 (sprints 11,12,13) = 4,7,7 -> mean 6.0. Committed 6 (038 1 + 036 5).
#
# WORKFLOW (working-agreements.md 2026-06-26): the PO implements the plan EXTERNALLY onto this
# branch; the orchestrator does NOT dispatch implementers. The PO triggers the back half ("do your
# review"); the orchestrator then diffs sprint-14-start..HEAD, runs the FULL DoD gate itself, runs
# the Opus spec + quality reviewers for the 3+ pt story (STORY-036), resolves the wiki forward-blast
# radius, then review/verdict/merge/retro. plan.md is the ONLY contract the implementer builds to.
#
# PIPELINE: STORY-038 (1 pt) -> GATE ONLY. STORY-036 (5 pts) -> GATE + Opus spec & quality reviewers
# (reviewers on Opus, working-agreements.md 2026-06-24).
#
# DoD CHANGE: STORY-038 adds a 5TH import-linter contract (src must not import tests) -> the
# command-sync agreement applies: .scrum/definition-of-done.md (lint-imports now 5 contracts) +
# CLAUDE.md (§4 contract count/list) updated in the SAME commit that lands it. From that commit on,
# `lint-imports` must report 5 kept / 0 broken. STORY-036 then EXTENDS the api-feature-independence
# module list (+ src.api.v1.maintenance) -> count stays 5, no further command-sync.
# NO new migration this sprint (maintenance_windows table exists from the spine schema, STORY-006).
#
# Baseline at lock: tree clean on main @ 0dc1d75 -> refined onto branch @ 03a3931. Cheap gates green
# at lock (ruff check + format clean; lint-imports 4 kept / 0 broken). DB gates recorded green at
# sprint-13 review (08c4eba) with no src/ change since. Conventions checklist (2026-06-27) + the
# five-file-shape-test agreement (2026-06-28) apply; the TOCTOU agreement (2026-06-28) is N/A
# (maintenance create is an INSERT, not a guarded conditional write). See plan.md per-story.
stories:
  - id: STORY-038
    points: 1
    board: done            # todo | in-progress | blocked | review | done
    pipeline: gate         # 1 pt -> (external impl) + DoD gate only
    blocked_question: null
    attempts: 0
    paused_at_commit: null
    dod_evidence:
      - command: "all SIX commands"
        exit_code: 0
        output_tail: "orchestrator-verified at 83af36a with throwaway DB: pytest 317 passed;
          lint-imports 5 kept / 0 broken (NEW src-no-tests forbidden contract live + non-vacuous
          spike); fk 0; alembic 0; ruff clean. command-sync done (definition-of-done.md + CLAUDE.md
          4->5 contracts; architecture-boundary + dev-setup-and-dod re-verified)."
        commit: cf1532a
        at: 2026-06-28T20:30:00+05:30
    blast_radius_resolved: true   # architecture-boundary (5th contract) + dev-setup-and-dod synced.
  - id: STORY-036
    points: 5
    board: done
    pipeline: gate+reviewers   # 5 pts -> (external impl) + Opus spec & quality reviewers + DoD gate
    blocked_question: null
    attempts: 0            # external impl interrupted at quota cutoff; finished by a Sonnet
                          # implementer subagent (PO-authorized), then orchestrator review.
    paused_at_commit: null
    dod_evidence:
      - command: "all SIX commands"
        exit_code: 0
        output_tail: "orchestrator-verified at 83af36a with throwaway DB: pytest 317 passed;
          lint-imports 5/0; fk 0; alembic 0; ruff clean. Opus spec reviewer: PASS (all 6 AC MET).
          Opus quality reviewer: APPROVE (0 critical / 0 major)."
        commit: 83af36a
        at: 2026-06-28T20:30:00+05:30
    review:
      second_pass: "spec PASS; quality APPROVE."
      open_minors:          # non-blocking -> retro / follow-up
        - "maintenance/service.py id=w.id if ... else 0 dead coercion (the same pattern removed in
          sprint 13 approvals; reintroduced by the Gemini implementer). Align to id=w.id."
        - "POST /maintenance has no actor field (story text mentioned self-declared actor; AC4 did not
          require it). Auth deferred anyway."
      note: "Gemini implementer left ruff violations in ~7 files + a stale dev-setup-and-dod (missed in
        STORY-038 blast radius); both caught + fixed by the Sonnet finisher before review."
    blast_radius_resolved: true   # canonical-types-and-ports, persistence-adapters,
                                  # api-five-file-convention, architecture-boundary re-verified @ 83af36a.
hotfixes: []
interrupts: []
